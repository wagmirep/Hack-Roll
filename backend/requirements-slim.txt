# requirements-slim.txt - Slim Python Dependencies (No ML Models)
#
# PURPOSE:
#     Lightweight dependencies for deployment when using external ML APIs.
#     ML models (transcription, diarization) run on Colab via API calls.
#     Install with: pip install -r requirements-slim.txt

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.25
psycopg2-binary>=2.9.9
alembic>=1.13.1

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# HTTP Client (for calling external ML APIs)
httpx>=0.26.0

# Configuration
python-dotenv>=1.0.0
pydantic>=2.5.3
pydantic-settings>=2.1.0

# Audio Processing (still needed for concatenation and segment extraction)
librosa>=0.10.1
soundfile>=0.12.0
numpy>=1.24.0
pydub>=0.25.1

# Queue
redis>=5.0.1

# Storage
boto3>=1.34.22

# Firebase
firebase-admin>=6.4.0

# Utilities
python-dateutil>=2.8.2

# =============================================================================
# EXCLUDED (running on Colab):
# - transformers, torch, torchaudio, accelerate (MERaLiON transcription)
# - pyannote.audio (speaker diarization)
# =============================================================================
